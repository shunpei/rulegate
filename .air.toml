root = "."
tmp_dir = "tmp"

[build]
  cmd = "go build -o ./tmp/api ./cmd/api"
  bin = "./tmp/api"
  include_ext = ["go", "md"]
  exclude_dir = ["tmp", "frontend", "node_modules", ".git"]
  delay = 1000

[log]
  time = false

[misc]
  clean_on_exit = true
